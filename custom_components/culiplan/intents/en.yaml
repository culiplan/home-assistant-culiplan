# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=en
# Phase 3 cooking-mode intents added manually (task-1397).
language: "en"
intents:
  FlavorplanWhatsDinnerTonight:
    data:
      - sentences:
          - "what's for dinner [tonight]"
          - "what am I cooking [today]"
          - "what's on the menu [today]"
          - "what did I plan for dinner"
          - "what's for lunch [today]"
  FlavorplanGetWeekMeals:
    data:
      - sentences:
          - "what's planned this week"
          - "what am I cooking this week"
          - "show me this week's meal plan"
          - "what about tomorrow"
          - "what's for dinner tomorrow"
  FlavorplanGetShoppingList:
    data:
      - sentences:
          - "what do I need to buy"
          - "show my shopping list"
          - "what's on my shopping list"
          - "what should I get at the store"
  FlavorplanAddToShoppingList:
    data:
      - sentences:
          - "add {item} to [my] shopping list"
          - "I need to buy {item}"
          - "put {item} on [my] list"
        slots:
          item: "{item}"
  FlavorplanWhatsInPantry:
    data:
      - sentences:
          - "what's in my pantry"
          - "what do I have at home"
          - "what can I cook with what I have"
  FlavorplanWhatsExpiringSoon:
    data:
      - sentences:
          - "what's expiring soon"
          - "what food is about to expire"
          - "what should I use up"
  FlavorplanNextCookingStep:
    data:
      - sentences:
          - "next step"
          - "next cooking step"
          - "what's the next step"
          - "go to the next step"
          - "continue cooking"
  FlavorplanSetRecipeTimer:
    data:
      - sentences:
          - "start [the] {label} timer [for] {duration_sec} seconds"
          - "start [the] {label} timer"
          - "set [a] {label} timer [for] {duration_sec} seconds"
          - "set [a] timer for {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  FlavorplanCancelRecipeTimer:
    data:
      - sentences:
          - "cancel [the] {label} timer"
          - "stop [the] {label} timer"
        slots:
          label: "{label}"

responses:
  intents:
    FlavorplanWhatsDinnerTonight: "{{ response }}"
    FlavorplanGetWeekMeals: "This week you have {{ slots.totalMealsPlanned }} meals planned."
    FlavorplanGetShoppingList: "{{ response }}"
    FlavorplanAddToShoppingList: "Added {{ slots.item }} to your shopping list."
    FlavorplanWhatsInPantry: "{{ response }}"
    FlavorplanWhatsExpiringSoon: "{{ response }}"
    FlavorplanNextCookingStep: "Moving to the next cooking step."
    FlavorplanSetRecipeTimer: "Starting the {{ slots.label }} timer."
    FlavorplanCancelRecipeTimer: "Cancelled the {{ slots.label }} timer."
