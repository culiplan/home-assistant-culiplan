# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=fr
language: "fr"
intents:
  CuliplanWhatsDinnerTonight:
    data:
      - sentences:
          - "qu'est-ce qu'on mange ce soir"
          - "qu'est-ce que je cuisine aujourd'hui"
          - "quel est le menu du jour"
  CuliplanGetWeekMeals:
    data:
      - sentences:
          - "qu'est-ce qui est prévu cette semaine"
          - "quel est le menu de cette semaine"
          - "qu'est-ce qu'on mange demain"
  CuliplanGetShoppingList:
    data:
      - sentences:
          - "qu'est-ce que je dois acheter"
          - "montrez-moi ma liste de courses"
          - "qu'est-ce qui est sur ma liste"
  CuliplanAddToShoppingList:
    data:
      - sentences:
          - "ajoutez {item} à [ma] liste de courses"
          - "j'ai besoin d'acheter {item}"
          - "mettez {item} sur [ma] liste"
        slots:
          item: "{item}"
  CuliplanWhatsInPantry:
    data:
      - sentences:
          - "qu'est-ce que j'ai dans mon garde-manger"
          - "qu'est-ce que j'ai à la maison"
          - "que puis-je cuisiner avec ce que j'ai"
  CuliplanWhatsExpiringSoon:
    data:
      - sentences:
          - "qu'est-ce qui expire bientôt"
          - "qu'est-ce que je dois utiliser rapidement"
  CuliplanNextCookingStep:
    data:
      - sentences:
          - "étape suivante"
          - "prochaine étape"
          - "quelle est la prochaine étape"
          - "continuer à cuisiner"
  CuliplanSetRecipeTimer:
    data:
      - sentences:
          - "démarrer le minuteur {label} [pour] {duration_sec} secondes"
          - "démarrer le minuteur {label}"
          - "mettre un minuteur {label} [pour] {duration_sec} secondes"
          - "mettre un minuteur pour {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  CuliplanCancelRecipeTimer:
    data:
      - sentences:
          - "annuler le minuteur {label}"
          - "arrêter le minuteur {label}"
        slots:
          label: "{label}"

responses:
  intents:
    CuliplanWhatsDinnerTonight: "{{ response }}"
    CuliplanGetWeekMeals: "{{ response }}"
    CuliplanGetShoppingList: "{{ response }}"
    CuliplanAddToShoppingList: "{{ slots.item }} ajouté à ta liste de courses."
    CuliplanWhatsInPantry: "{{ response }}"
    CuliplanWhatsExpiringSoon: "{{ response }}"
    CuliplanNextCookingStep: "Passage à l'étape suivante."
    CuliplanSetRecipeTimer: "Minuteur {{ slots.label }} démarré."
    CuliplanCancelRecipeTimer: "Minuteur {{ slots.label }} annulé."
