# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=nl
# CuliplanAddToPantry (+ lists) added manually — pantry add via Assist.
language: "nl"
lists:
  # Vrije tekst voor het artikel; de ingebouwde intents delen deze lijst niet
  # met custom-sentence bestanden, dus hier als wildcard declareren.
  item:
    wildcard: true
  # Gesproken bewaarplaats → backend PantryStock.location waarde.
  culiplan_location:
    values:
      - in: "(voorraadkast|voorraad|provisiekast|kast|berging)"
        out: "pantry"
      - in: "(koelkast|frigo|ijskast)"
        out: "fridge"
      - in: "(diepvries|diepvriezer|vriezer|vriesvak|vrieskast)"
        out: "freezer"
      - in: "(aanrecht|werkblad)"
        out: "counter"
      - in: "(kruidenrek|kruidenkast)"
        out: "spice_rack"
intents:
  CuliplanWhatsDinnerTonight:
    data:
      - sentences:
          - "wat eet ik vanavond"
          - "wat staat er op het menu vandaag"
          - "wat heb ik gepland voor vandaag"
          - "wat eet ik vandaag"
  CuliplanGetWeekMeals:
    data:
      - sentences:
          - "wat staat er deze week gepland"
          - "wat kook ik deze week"
          - "wat is het menu voor morgen"
  CuliplanGetShoppingList:
    data:
      - sentences:
          - "wat moet ik nog kopen"
          - "wat staat er op mijn boodschappenlijstje"
          - "toon mijn boodschappenlijst"
  CuliplanAddToShoppingList:
    data:
      - sentences:
          - "voeg {item} toe aan [mijn] boodschappenlijst"
          - "ik moet {item} kopen"
          - "zet {item} op [mijn] lijst"
        slots:
          item: "{item}"
  CuliplanAddToPantry:
    data:
      - sentences:
          - "voeg {item} toe aan [de|mijn] {culiplan_location:location}"
          - "(zet|leg|stop|doe) {item} in [de|mijn] {culiplan_location:location}"
          - "ik heb {item} in [de|mijn] {culiplan_location:location}"
        slots:
          item: "{item}"
      - sentences:
          - "ik heb {item} gekocht"
        slots:
          item: "{item}"
          location: "pantry"
  CuliplanWhatsInPantry:
    data:
      - sentences:
          - "wat heb ik in de voorraadkast"
          - "wat heb ik thuis"
          - "wat kan ik maken met wat ik heb"
  CuliplanWhatsExpiringSoon:
    data:
      - sentences:
          - "wat verloopt er binnenkort"
          - "wat moet ik snel gebruiken"
          - "wat is bijna verlopen"
  CuliplanNextCookingStep:
    data:
      - sentences:
          - "volgende stap"
          - "ga naar de volgende stap"
          - "wat is de volgende stap"
          - "verder koken"
  CuliplanSetRecipeTimer:
    data:
      - sentences:
          - "start [de] {label} timer [van] {duration_sec} seconden"
          - "start [de] {label} timer"
          - "zet [een] {label} timer [van] {duration_sec} seconden"
          - "zet een timer voor {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  CuliplanCancelRecipeTimer:
    data:
      - sentences:
          - "stop [de] {label} timer"
          - "annuleer [de] {label} timer"
        slots:
          label: "{label}"

responses:
  intents:
    CuliplanWhatsDinnerTonight: "{{ response }}"
    CuliplanGetWeekMeals: "{{ response }}"
    CuliplanGetShoppingList: "{{ response }}"
    CuliplanAddToShoppingList: "{{ slots.item }} toegevoegd aan je boodschappenlijst."
    CuliplanAddToPantry: "{{ slots.item }} toegevoegd aan je voorraad."
    CuliplanWhatsInPantry: "{{ response }}"
    CuliplanWhatsExpiringSoon: "{{ response }}"
    CuliplanNextCookingStep: "Naar de volgende kookstap."
    CuliplanSetRecipeTimer: "De {{ slots.label }} timer gestart."
    CuliplanCancelRecipeTimer: "De {{ slots.label }} timer gestopt."
