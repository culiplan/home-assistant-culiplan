# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=de
# CuliplanAddToPantry (+ lists) added manually — pantry add via Assist.
language: "de"
lists:
  # Freitext für den Artikel; die eingebauten Intents stellen diese Liste
  # nicht für Custom-Sentence-Dateien bereit, daher hier als Wildcard.
  item:
    wildcard: true
  # Gesprochener Lagerort → Backend-Wert PantryStock.location.
  culiplan_location:
    values:
      - in: "(Vorratskammer|Vorrat|Vorräte|Speisekammer|Vorratsschrank)"
        out: "pantry"
      - in: "Kühlschrank"
        out: "fridge"
      - in: "(Gefrierschrank|Gefriertruhe|Gefrierfach|Tiefkühler|Tiefkühltruhe)"
        out: "freezer"
      - in: "(Arbeitsplatte|Theke|Küchentheke)"
        out: "counter"
      - in: "(Gewürzregal|Gewürzschrank)"
        out: "spice_rack"
intents:
  CuliplanWhatsDinnerTonight:
    data:
      - sentences:
          - "was gibt es heute zum Abendessen"
          - "was koche ich heute"
          - "was steht heute auf dem Speiseplan"
  CuliplanGetWeekMeals:
    data:
      - sentences:
          - "was ist diese Woche geplant"
          - "was koche ich diese Woche"
          - "was gibt es morgen"
  CuliplanGetShoppingList:
    data:
      - sentences:
          - "was muss ich einkaufen"
          - "was steht auf meiner Einkaufsliste"
          - "zeig mir meine Einkaufsliste"
  CuliplanAddToShoppingList:
    data:
      - sentences:
          - "füge {item} zur Einkaufsliste hinzu"
          - "ich brauche {item}"
          - "setze {item} auf [meine] Liste"
        slots:
          item: "{item}"
  CuliplanAddToPantry:
    data:
      - sentences:
          - "füge {item} (zum|zur|in den|in die|ins|in das|zu meinem|zu meiner) {culiplan_location:location} [hinzu]"
          - "(lege|leg|tu|tue|stelle|stell) {item} (in den|in die|ins|in das|auf die) {culiplan_location:location}"
          - "ich habe {item} (im|in der|auf der) {culiplan_location:location}"
        slots:
          item: "{item}"
      - sentences:
          - "ich habe {item} gekauft"
        slots:
          item: "{item}"
          location: "pantry"
  CuliplanWhatsInPantry:
    data:
      - sentences:
          - "was habe ich in der Speisekammer"
          - "was habe ich zuhause"
          - "was kann ich mit dem kochen was ich habe"
  CuliplanWhatsExpiringSoon:
    data:
      - sentences:
          - "was läuft bald ab"
          - "was sollte ich bald verwenden"
          - "was ist fast abgelaufen"
  CuliplanNextCookingStep:
    data:
      - sentences:
          - "nächster Schritt"
          - "zum nächsten Schritt"
          - "was ist der nächste Schritt"
          - "weiter kochen"
  CuliplanSetRecipeTimer:
    data:
      - sentences:
          - "starte [den] {label} Timer [für] {duration_sec} Sekunden"
          - "starte [den] {label} Timer"
          - "setze [einen] {label} Timer [für] {duration_sec} Sekunden"
          - "setze einen Timer für {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  CuliplanCancelRecipeTimer:
    data:
      - sentences:
          - "stoppe [den] {label} Timer"
          - "abbrechen [den] {label} Timer"
        slots:
          label: "{label}"

responses:
  intents:
    CuliplanWhatsDinnerTonight: "{{ response }}"
    CuliplanGetWeekMeals: "{{ response }}"
    CuliplanGetShoppingList: "{{ response }}"
    CuliplanAddToShoppingList: "{{ slots.item }} zu deiner Einkaufsliste hinzugefügt."
    CuliplanAddToPantry: "{{ slots.item }} zum Vorrat hinzugefügt."
    CuliplanWhatsInPantry: "{{ response }}"
    CuliplanWhatsExpiringSoon: "{{ response }}"
    CuliplanNextCookingStep: "Zum nächsten Kochschritt."
    CuliplanSetRecipeTimer: "Der {{ slots.label }} Timer gestartet."
    CuliplanCancelRecipeTimer: "Der {{ slots.label }} Timer gestoppt."
