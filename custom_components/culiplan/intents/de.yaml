# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=de
language: "de"
intents:
  FlavorplanWhatsDinnerTonight:
    data:
      - sentences:
          - "was gibt es heute zum Abendessen"
          - "was koche ich heute"
          - "was steht heute auf dem Speiseplan"
  FlavorplanGetWeekMeals:
    data:
      - sentences:
          - "was ist diese Woche geplant"
          - "was koche ich diese Woche"
          - "was gibt es morgen"
  FlavorplanGetShoppingList:
    data:
      - sentences:
          - "was muss ich einkaufen"
          - "was steht auf meiner Einkaufsliste"
          - "zeig mir meine Einkaufsliste"
  FlavorplanAddToShoppingList:
    data:
      - sentences:
          - "füge {item} zur Einkaufsliste hinzu"
          - "ich brauche {item}"
          - "setze {item} auf [meine] Liste"
        slots:
          item: "{item}"
  FlavorplanWhatsInPantry:
    data:
      - sentences:
          - "was habe ich in der Speisekammer"
          - "was habe ich zuhause"
          - "was kann ich mit dem kochen was ich habe"
  FlavorplanWhatsExpiringSoon:
    data:
      - sentences:
          - "was läuft bald ab"
          - "was sollte ich bald verwenden"
          - "was ist fast abgelaufen"
  FlavorplanNextCookingStep:
    data:
      - sentences:
          - "nächster Schritt"
          - "zum nächsten Schritt"
          - "was ist der nächste Schritt"
          - "weiter kochen"
  FlavorplanSetRecipeTimer:
    data:
      - sentences:
          - "starte [den] {label} Timer [für] {duration_sec} Sekunden"
          - "starte [den] {label} Timer"
          - "setze [einen] {label} Timer [für] {duration_sec} Sekunden"
          - "setze einen Timer für {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  FlavorplanCancelRecipeTimer:
    data:
      - sentences:
          - "stoppe [den] {label} Timer"
          - "abbrechen [den] {label} Timer"
        slots:
          label: "{label}"

responses:
  intents:
    FlavorplanWhatsDinnerTonight: "{{ response }}"
    FlavorplanGetWeekMeals: "{{ response }}"
    FlavorplanGetShoppingList: "{{ response }}"
    FlavorplanAddToShoppingList: "{{ slots.item }} zu deiner Einkaufsliste hinzugefügt."
    FlavorplanWhatsInPantry: "{{ response }}"
    FlavorplanWhatsExpiringSoon: "{{ response }}"
    FlavorplanNextCookingStep: "Zum nächsten Kochschritt."
    FlavorplanSetRecipeTimer: "Der {{ slots.label }} Timer gestartet."
    FlavorplanCancelRecipeTimer: "Der {{ slots.label }} Timer gestoppt."
