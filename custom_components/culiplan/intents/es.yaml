# Auto-generated by scripts/generate_intents.py — do not edit manually.
# Source: api.culiplan.com/api/voice/tools?tag=ha-assist:eligible&lang=es
language: "es"
intents:
  FlavorplanWhatsDinnerTonight:
    data:
      - sentences:
          - "¿qué hay para cenar esta noche?"
          - "¿qué cocino hoy?"
          - "¿qué tengo planeado para hoy?"
  FlavorplanGetWeekMeals:
    data:
      - sentences:
          - "¿qué está planeado esta semana?"
          - "¿qué cocino esta semana?"
          - "¿qué hay para mañana?"
  FlavorplanGetShoppingList:
    data:
      - sentences:
          - "¿qué necesito comprar?"
          - "muéstrame mi lista de compras"
          - "¿qué hay en mi lista?"
  FlavorplanAddToShoppingList:
    data:
      - sentences:
          - "agregar {item} a [mi] lista de compras"
          - "necesito comprar {item}"
          - "pon {item} en [mi] lista"
        slots:
          item: "{item}"
  FlavorplanWhatsInPantry:
    data:
      - sentences:
          - "¿qué tengo en mi despensa?"
          - "¿qué tengo en casa?"
          - "¿qué puedo cocinar con lo que tengo?"
  FlavorplanWhatsExpiringSoon:
    data:
      - sentences:
          - "¿qué vence pronto?"
          - "¿qué debo usar pronto?"
          - "¿qué está a punto de caducar?"
  FlavorplanNextCookingStep:
    data:
      - sentences:
          - "siguiente paso"
          - "próximo paso"
          - "¿cuál es el siguiente paso?"
          - "continuar cocinando"
  FlavorplanSetRecipeTimer:
    data:
      - sentences:
          - "iniciar el temporizador {label} [de] {duration_sec} segundos"
          - "iniciar el temporizador {label}"
          - "poner un temporizador {label} [de] {duration_sec} segundos"
          - "poner un temporizador para {label}"
        slots:
          label: "{label}"
          duration_sec: "{duration_sec}"
  FlavorplanCancelRecipeTimer:
    data:
      - sentences:
          - "cancelar el temporizador {label}"
          - "detener el temporizador {label}"
        slots:
          label: "{label}"

responses:
  intents:
    FlavorplanWhatsDinnerTonight: "{{ response }}"
    FlavorplanGetWeekMeals: "{{ response }}"
    FlavorplanGetShoppingList: "{{ response }}"
    FlavorplanAddToShoppingList: "{{ slots.item }} agregado a tu lista de compras."
    FlavorplanWhatsInPantry: "{{ response }}"
    FlavorplanWhatsExpiringSoon: "{{ response }}"
    FlavorplanNextCookingStep: "Avanzando al siguiente paso de cocina."
    FlavorplanSetRecipeTimer: "Temporizador {{ slots.label }} iniciado."
    FlavorplanCancelRecipeTimer: "Temporizador {{ slots.label }} cancelado."
